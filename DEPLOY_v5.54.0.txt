坡南寻宝记后台 v5.54.0 部署说明

1. 用本包文件覆盖 Zeabur 当前后台项目代码后重新部署。
2. 保留现有环境变量和数据卷。MINIGAME_DATA_DIR 建议保持为 /data。
3. 确认 Zeabur 持久化卷挂载到 /data；上传音乐保存在 /data/media/bgm。
4. 进入 /admin → 运行配置，上传并试听音乐，选择“后台上传”，然后保存全部配置。
5. 若使用网络地址，必须填写以 https:// 开头且可公开访问的音频地址。
6. 建议优先上传体积较小的 MP3，以减少玩家首次加载等待。
